import React, { useRef } from "react";

import "./helpers/DateManagement/extensions";
import "./helpers/TypeManagement/extensions";

export default function useScripts() {
    

	return useRef(
        <>
        
        </>
    ).current;
}
