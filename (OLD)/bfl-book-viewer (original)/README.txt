SETUP
    Use "npm install" to install node modules to project folder based on dependencies listed in package.json.

LIVE
    Use "npm run dev" to quickly make build.
    Use "npm run build" to run optimized build.
    Press [CTRL+C] and select yes to end process.
    *"dev" and "build" commands are listed in package.json scripts section.